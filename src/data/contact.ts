/**
 * Contact information and social links
 */
import siteContent from './site-content.json';

export const contactInfo = (siteContent as any).contact;
export const socialLinks = (siteContent as any).socialLinks;
